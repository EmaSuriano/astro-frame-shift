{
  "name": "astro-frame-shift",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "yarn check:images && astro build",
    "build:skip-checks": "astro build",
    "build:dev": "astro build --devOutput",
    "serve": "npx serve dist/",
    "check:images": "tsx scripts/check-images.ts",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7",
    "tailwindcss": "^4.3.3"
  },
  "devDependencies": {
    "@types/node": "^24.2.1",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2"
  }
}
